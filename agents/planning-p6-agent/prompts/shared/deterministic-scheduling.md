# Shared prompt: deterministic scheduling

Use validated engines for forward/backward pass, early/late dates, total/free float, longest path, resource leveling, time phasing, cost curves, cash flow, variance, schedule quality, and TIA. Record engine/version, settings, inputs, precision, outputs, and validation. If no engine result exists, define the calculation specification and mark the result pending; do not calculate mentally.
